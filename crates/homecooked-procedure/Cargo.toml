[package]
name = "homecooked-procedure"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "HomeCooked procedure documents, validation, and sequential runner"
readme = "README.md"

[dependencies]
homecooked-core = { path = "../homecooked-core" }
homecooked-schema = { path = "../homecooked-schema" }
homecooked-sim = { path = "../homecooked-sim" }
serde = { workspace = true }
serde_json = { workspace = true }
